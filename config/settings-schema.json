{
  "schemaVersion": 1,
  "commands": ["help", "agent-guide", "status", "doctor", "config.schema", "config.get", "config.set", "config.apply", "config.reset", "config.retry", "config.export"],
  "settings": {
    "iconSize": {"type":"integer","minimum":24,"maximum":96,"description":"Base application icon size in logical pixels."},
    "magnification": {"type":"number","minimum":1,"maximum":2,"effectiveStep":0.05,"description":"Hover icon scale; requested fractions are retained."},
    "magnificationRadius": {"type":"number","minimum":40,"maximum":240,"effectiveStep":5,"description":"Hover influence radius in logical pixels."},
    "hoverGlowEnabled": {"type":"boolean","description":"Enable glow behind hovered application icons."},
    "hoverGlowOpacity": {"type":"number","minimum":0,"maximum":1,"effectiveStep":0.05,"description":"Glow opacity. Requested 0.72 renders as 0.70."},
    "hoverGlowRadius": {"type":"number","minimum":0,"maximum":100,"effectiveStep":5,"description":"Glow radius; active when hoverGlowEnabled is true."},
    "showPreviews": {"type":"boolean","description":"Allow grouped-window previews."},
    "showTrash": {"type":"boolean","description":"Show Trash and enable its existing polling; does not empty Trash."},
    "margin": {"type":"integer","minimum":0,"resetWithPreferences":false,"description":"Edge margin; retained by preference reset."},
    "backgroundOpacity": {"type":"number","minimum":0,"maximum":1,"effectiveStep":0.05,"description":"Multiplier of the effective background color alpha."},
    "backgroundColorEnabled": {"type":"boolean","description":"Enable the configured background color override."},
    "backgroundColor": {"type":"string","format":"color","description":"Empty, #RRGGBB, Qt #AARRGGBB or @theme.token; active only with backgroundColorEnabled."},
    "borderColorEnabled": {"type":"boolean","description":"Enable the configured border color override."},
    "borderColor": {"type":"string","format":"color","description":"Empty, #RRGGBB, Qt #AARRGGBB or @theme.token; active only with borderColorEnabled."},
    "workspaceBadgeBackgroundColorEnabled": {"type":"boolean","description":"Enable the workspace badge background override."},
    "workspaceBadgeBackgroundColor": {"type":"string","format":"color","description":"Empty, #RRGGBB, Qt #AARRGGBB or @theme.token; requires its paired Enabled flag."},
    "workspaceBadgeTextColorEnabled": {"type":"boolean","description":"Enable the workspace badge text override."},
    "workspaceBadgeTextColor": {"type":"string","format":"color","description":"Empty, #RRGGBB, Qt #AARRGGBB or @theme.token; requires its paired Enabled flag."},
    "borderWidthEnabled": {"type":"boolean","description":"Enable a fixed width instead of the theme's border widths."},
    "borderWidth": {"type":"integer","minimum":0,"maximum":8,"description":"Fixed logical-pixel border width; theme-owned while borderWidthEnabled is false."},
    "position": {"type":"string","enum":["top","bottom","left","right"],"description":"Dock edge. Left/right temporarily render workspaceLayout as flat."},
    "fullLength": {"type":"boolean","description":"Extend the dock along its available screen edge."},
    "reserveSpace": {"type":"boolean","description":"Reserve an exclusive zone; effective false while autoHide is enabled."},
    "autoHide": {"type":"boolean","description":"Auto-hide the dock without changing the requested reserveSpace value."},
    "clickAction": {"type":"string","enum":["none","minimize-restore","previews","close","focus-or-launch"],"legacyReadAliases":{"focus":"focus-or-launch","launch":"focus-or-launch"},"description":"Left-click action. close may close application windows when used."},
    "middleClickAction": {"type":"string","enum":["none","minimize-restore","previews","close","focus-or-launch"],"legacyReadAliases":{"focus":"focus-or-launch","launch":"focus-or-launch"},"description":"Middle-click action. New writes use canonical action names."},
    "scrollAction": {"type":"string","enum":["none","cycle-windows"],"description":"Scroll behavior for applications with multiple windows."},
    "controlCommand": {"type":"string","minLength":1,"risk":"executes-on-use","description":"Launcher command executed only by the existing launcher action, never by validation or discovery."},
    "sortByWorkspace": {"type":"boolean","description":"Sort flat-layout applications by their lowest occupied workspace."},
    "workspaceLayout": {"type":"string","enum":["flat","grouped"],"description":"Grouped cards are supported on top/bottom. Requested grouped survives a move to left/right."},
    "workspaceMonitorScope": {"type":"string","enum":["all","current-monitor"],"description":"Workspace-card monitor scope; inactive in flat layout."},
    "groupWindows": {"type":"boolean","description":"Group windows belonging to the same application."},
    "interfaceAnimationsEnabled": {"type":"boolean","description":"Enable interface transitions; separate from urgent-window attention motion."},
    "windowScope": {"type":"string","enum":["all","workspace","monitor","workspace-monitor"],"description":"Running-window scope, independent of pinned launchers and hidden applications."},
    "showUrgentOutsideScope": {"type":"boolean","description":"Allow genuinely urgent windows outside the selected scope; hidden applications remain hidden."},
    "attentionBadgesEnabled": {"type":"boolean","description":"Enable attention indicators without starting a notification daemon."},
    "urgentWindowAnimationEnabled": {"type":"boolean","description":"Enable the existing urgent-window attention motion."},
    "launcherBadgeMode": {"type":"string","enum":["automatic","dots-only"],"description":"Use available provider counts or existing dot fallback; does not install or start a provider."},
    "hiddenApplications": {"type":"array","format":"application-ids","resetWithPreferences":false,"description":"Hidden desktop IDs; independent of pins. Retained by preference reset."},
    "pinned": {"type":"array","format":"application-ids","resetWithPreferences":false,"description":"Ordered desktop IDs, including unavailable/hidden apps. Retained by preference reset."}
  }
}
